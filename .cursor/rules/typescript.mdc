---
description: "TypeScript 类型定义规范，包括类型检查、类型守卫和导入导出"
alwaysApply: true
---

# TypeScript 规范

## 类型定义
- 严格类型检查，启用 `strict: true`
- **禁止使用 `any`**，使用 `unknown` 替代，需要时进行类型守卫
- 为所有函数参数和返回值定义类型
- 使用接口（`interface`）定义对象类型
- 使用类型别名（`type`）定义联合类型、交叉类型等
- 使用泛型提高代码复用性

## 类型守卫
- 使用类型守卫处理 `unknown` 类型

## 导入导出
- 使用路径别名 `@/` 导入模块，不要使用相对路径
- 类型导入使用 `import type`
